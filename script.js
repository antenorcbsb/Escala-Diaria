async function loadPdfList() { try { setStatus( 'loading', 'A consultar PDFs...' ); const response = await fetch( APPS_SCRIPT_URL + '?t=' + Date.now() ); if (!response.ok) { throw new Error( 'Erro HTTP ' + response.status ); } const data = await response.json(); if (!data.success) { throw new Error( 'O Google Apps Script devolveu um erro.' ); } /* * Guarda a lista de PDFs */ pdfList = data.files || {}; console.log( 'Lista de PDFs:', pdfList ); /* * Mostra a quantidade * de PDFs encontrados */ console.log( 'PDFs encontrados:', Object.keys(pdfList).length ); return true; } catch (error) { console.error( 'Erro ao obter PDFs:', error ); setStatus( 'error', 'Erro ao consultar os PDFs' ); return false; } }
